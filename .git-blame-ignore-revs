# Revisions that are pure formatting. `git blame` should skip them.
#
# Configure once per clone:
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub honours this file automatically. It matters here because task 1.2 imported
# packages/ocean with 193 commits of history specifically to preserve provenance, and a
# wholesale reformat would otherwise attribute all 429 files to the reformat commit.

# style: reformat packages/ocean under the monorepo formatter (task 1.3)
d124df70c9aa480f54e41b926284668a80050197
