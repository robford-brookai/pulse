{
  "name": "@pulse/twenty-app",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "description": "Twenty app: the PULSE views, navigation, and projection logic as code (objects and roles are artifact-owned)",
  "scripts": {
    "test": "vitest run",
    "test:live": "vitest run --config vitest.live.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "twenty-sdk": "2.30.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
