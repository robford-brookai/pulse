{
  "schedules": {
    "month-open": {
      "cron_expression": "cron(30 0 1 * ? *)",
      "description": "D16 billing-month open: enumerates active/on-hold Enrollments and declares open_billing_episode for the current month.",
      "target_subcommand": "month-open",
      "maximum_retry_attempts": 3,
      "maximum_event_age_in_seconds": 82800
    },
    "consent-sweep": {
      "cron_expression": "rate(1 day)",
      "description": "D9 consent reconciliation: diffs the delivered Customer.io suppression export against ledger CommunicationConsent state and declares corrections.",
      "target_subcommand": "consent-sweep",
      "maximum_retry_attempts": 3,
      "maximum_event_age_in_seconds": 86400
    },
    "reconcile-sweep-billing_episode": {
      "cron_expression": "rate(1 day)",
      "description": "Projection conformance sweep for the ledger-owned billing_episode family: compares projections against the ledger and reports divergences; never writes.",
      "target_subcommand": "reconcile-sweep",
      "target_argument": "billing_episode",
      "maximum_retry_attempts": 3,
      "maximum_event_age_in_seconds": 86400
    },
    "reconcile-sweep-consent": {
      "cron_expression": "rate(1 day)",
      "description": "Projection conformance sweep for the ledger-owned consent family: compares projections against the ledger and reports divergences; never writes.",
      "target_subcommand": "reconcile-sweep",
      "target_argument": "consent",
      "maximum_retry_attempts": 3,
      "maximum_event_age_in_seconds": 86400
    },
    "reconcile-sweep-contract": {
      "cron_expression": "rate(1 day)",
      "description": "Projection conformance sweep for the ledger-owned contract family: compares projections against the ledger and reports divergences; never writes.",
      "target_subcommand": "reconcile-sweep",
      "target_argument": "contract",
      "maximum_retry_attempts": 3,
      "maximum_event_age_in_seconds": 86400
    },
    "reconcile-sweep-coverage": {
      "cron_expression": "rate(1 day)",
      "description": "Projection conformance sweep for the ledger-owned coverage family: compares projections against the ledger and reports divergences; never writes.",
      "target_subcommand": "reconcile-sweep",
      "target_argument": "coverage",
      "maximum_retry_attempts": 3,
      "maximum_event_age_in_seconds": 86400
    },
    "reconcile-sweep-device": {
      "cron_expression": "rate(1 day)",
      "description": "Projection conformance sweep for the ledger-owned device family: compares projections against the ledger and reports divergences; never writes.",
      "target_subcommand": "reconcile-sweep",
      "target_argument": "device",
      "maximum_retry_attempts": 3,
      "maximum_event_age_in_seconds": 86400
    },
    "reconcile-sweep-enrollment": {
      "cron_expression": "rate(1 day)",
      "description": "Projection conformance sweep for the ledger-owned enrollment family: compares projections against the ledger and reports divergences; never writes.",
      "target_subcommand": "reconcile-sweep",
      "target_argument": "enrollment",
      "maximum_retry_attempts": 3,
      "maximum_event_age_in_seconds": 86400
    },
    "reconcile-sweep-referral": {
      "cron_expression": "rate(1 day)",
      "description": "Projection conformance sweep for the ledger-owned referral family: compares projections against the ledger and reports divergences; never writes.",
      "target_subcommand": "reconcile-sweep",
      "target_argument": "referral",
      "maximum_retry_attempts": 3,
      "maximum_event_age_in_seconds": 86400
    }
  }
}
